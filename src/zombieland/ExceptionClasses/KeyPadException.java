/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package zombieland.ExceptionClasses;

import java.util.Scanner;

/**
 *
 * @author K-Reed
 */
public class KeyPadException { 
    try{     
        inFile.nextLine(null);{
    }
    }
   catch (while (!"0".equals(numin) && !"1".equals(numin) && !"2".equals(numin) && !"3".equals(numin) && !"4".equals(numin) && !"5".equals(numin) && !"6".equals(numin) && !"7".equals(numin) && !"8".equals(numin) && !"9".equals(numin))){
      Error.displayErrorMsg("Error Try Again");
   }  
       finally{Zombieland.keyPadGetIn();
    }
   }
   
